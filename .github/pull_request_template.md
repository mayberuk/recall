## What changed and why

## Test tier run

Which tier from CONTRIBUTING.md's table did you run, and what was the result?

- [ ] Unit — `go test ./...`
- [ ] Race — `go test -race ./...`
- [ ] Integration — `go test ./tests/integration/...`
- [ ] Acceptance — `./tests/acceptance/run.sh`

If you touched `internal/scan`, `internal/archive`, or `internal/strip`, paste the
`RECALL_REAL_CORPUS=1` numbers here too.
